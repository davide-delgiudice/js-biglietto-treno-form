// richiamo gli elementi che mi servono

const form = document.querySelector('form');
const nameField = document.getElementById('name-field');
const kmField = parseInt(prompt(document.getElementById('km-field')));
const ageField = parseInt(prompt(document.getElementById('age-field')));